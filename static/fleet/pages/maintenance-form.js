// STUB — replaced in Task 11
export async function renderMaintenanceForm(id, prefill) {
  const { setContent } = await import('../utils/dom.js');
  setContent('<div>Maintenance form (todo)</div>');
}
